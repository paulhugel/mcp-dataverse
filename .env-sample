# Sample environment variables for local development
# Copy to `.env` and adjust as needed. Do not commit secrets.

# Cache directory (mounted by docker-compose)
CACHEDIR=./cache

# Dataverse installations index for overview endpoints
# Core tools do not require this
DATAVERSES=https://iqss.github.io/dataverse-installations/data/data.json
